# 实验部分

## 4.1 实验设置

**数据集与模型。** 为验证 LFCM 的泛化性，我们在 CIFAR-10 与 CIFAR-100 两个数据集上进行实验。CIFAR-10 包含 10 个类别，训练集 50,000 张图像，测试集 10,000 张图像；CIFAR-100 包含 100 个类别，样本规模与 CIFAR-10 相同。OOD 鲁棒性评估在对应的 CIFAR-10-C 与 CIFAR-100-C 数据集上进行，二者均包含 19 种不同类型的图像损坏，每种损坏分为 5 个严重等级。CIFAR-10 上的骨干网络为 WideResNet-34-10（WRN-34-10，34 层、宽度因子 10），CIFAR-100 上的骨干网络为 WRN-34-100。

**训练设置。** 所有对抗训练方法均采用 PGD-10 生成对抗样本进行训练，扰动强度 $\epsilon = 8/255$，步长 $\alpha = 2/255$。优化器使用 SGD，动量为 0.9，权重衰减为 $5\times10^{-4}$，初始学习率为 0.1，batch size 为 128。训练总轮数为 110 个 epoch，学习率在第 100 和 105 个 epoch 分别衰减为原来的 0.1 倍。Natural 训练（ST）不使用对抗样本，其余设置保持一致。

**LFCM 训练策略。** LFCM 采用三阶段训练范式：第一阶段（第 1-30 epoch）为预热阶段，仅使用交叉熵损失与掩码约束损失进行训练，不引入规范化相关损失；第二阶段（第 31-60 epoch）逐步引入 commitment 损失与多样性损失，学习 codebook 的低频编码表示；第三阶段（第 61-110 epoch）引入规范化对齐损失，以 $w_{\text{canon}}=0.5$（默认设置）的权重迫使干净样本与对抗样本的低频编码趋于一致。温度参数 $\tau$ 从初始值 2.0 线性退火至 0.5（第 30-100 epoch），编码分配逐步由软量化过渡至硬量化；每 5 个 epoch 检测并重置死码。LFCM 的总体损失函数为：

$$
\mathcal{L}_{\text{LFCM}} = \mathcal{L}_{\text{CE}} + \lambda_{\text{mask}} \mathcal{L}_{\text{mask}} + \lambda_{\text{commit}} \mathcal{L}_{\text{commit}} + \lambda_{\text{div}} \mathcal{L}_{\text{div}} + w_{\text{canon}} \mathcal{L}_{\text{canon}}
$$

其中 $\mathcal{L}_{\text{CE}}$ 为交叉熵损失，$\mathcal{L}_{\text{mask}}$ 为频率分离掩码约束损失，$\mathcal{L}_{\text{commit}}$ 与 $\mathcal{L}_{\text{div}}$ 分别为 codebook 的承诺损失与多样性损失，$\mathcal{L}_{\text{canon}}$ 为低频规范化对齐损失。

**评估协议。** 白盒对抗鲁棒性评估采用 PGD-1（单步）、PGD-20（20 步）、PGD-100（100 步）、CW-20（20 步 C&W 攻击）以及 AutoAttack（AA），所有攻击的扰动强度 $\epsilon = 8/255$。OOD 评估在 19 种损坏类型上测试清洁准确率，衡量模型在分布外损坏下的泛化能力。CIFAR-10 上的模型均取验证集鲁棒准确率最优的 checkpoint 进行评估；CIFAR-100 上对抗鲁棒性在最终模型上评估，OOD 鲁棒性在验证最优模型上评估（两种 checkpoint 下的性能差异极小，不影响结论）。

## 4.2 标准对抗鲁棒性对比

### 4.2.1 CIFAR-10 上的结果

表 1 展示了各方法在 CIFAR-10 测试集上的清洁准确率与白盒对抗鲁棒性。

| 方法 | Clean | PGD-1 | PGD-20 | PGD-100 | CW-20 | AA |
|:-----|:-----:|:-----:|:------:|:-------:|:-----:|:--:|
| Natural | 66.46 | 35.52 | 20.45 | 20.12 | 19.41 | 16.35 |
| AT | 85.65 | 80.89 | 56.51 | **56.03** | 55.03 | 53.61 |
| HFDR | 85.90 | 81.20 | 56.34 | 55.71 | 54.83 | 53.56 |
| LFCM (K=64) | 85.61 | 81.16 | **56.90** | 56.02 | **55.50** | **53.67** |

**表 1.** CIFAR-10 上各方法的清洁准确率与白盒对抗鲁棒性（%）。最佳结果以粗体标出。AA 表示 AutoAttack。

Natural 训练在清洁图像上仅取得 66.46% 的准确率，在 AutoAttack 下骤降至 16.35%，表明其对对抗攻击的高度脆弱性。AT 方法通过 PGD-10 对抗训练将 AutoAttack 鲁棒性提升至 53.61%，同时保持 85.65% 的清洁准确率。HFDR 在 AT 基础上引入高频特征解耦与重校准模块，将清洁准确率提升至 85.90%。LFCM 在 PGD-20、CW-20 与 AutoAttack 三项指标上均取得最优结果，分别为 56.90%、55.50% 与 53.67%，PGD-100 上与 AT 基本持平（56.02% vs. 56.03%）。相较于 AT，LFCM 在保持相当清洁准确率（85.61% vs. 85.65%）的同时，将 AutoAttack 准确率提升 0.06%；相较于 HFDR，LFCM 在 PGD-20 与 CW-20 上分别提升 0.56% 和 0.67%。更重要的是，LFCM 的清洁准确率与 AutoAttack 准确率之差（$\Delta$ Clean$-$AA）为 31.94%，低于 AT（32.04%）与 HFDR（32.34%），表明 LFCM 在清洁准确率与鲁棒性之间取得了更优的权衡。上述结果表明，对低频分量进行规范化能够在不牺牲清洁准确率的前提下进一步提升对抗鲁棒性。

### 4.2.2 CIFAR-100 上的结果

表 2 展示了各方法在 CIFAR-100 上的对比结果。

| 方法 | Clean | PGD-1 | PGD-20 | PGD-100 | CW-20 | AA |
|:-----|:-----:|:-----:|:------:|:-------:|:-----:|:--:|
| Natural | 79.67 | 20.86 | 5.51 | 4.86 | 4.83 | 4.16 |
| AT | **61.98** | 55.55 | 32.85 | **32.66** | 31.06 | 29.57 |
| HFDR | 61.30 | 55.40 | 32.99 | 32.62 | **31.18** | **29.84** |
| LFCM (K=64) | 61.27 | 55.53 | **33.14** | 32.62 | **31.18** | 29.82 |

**表 2.** CIFAR-100 上各方法的清洁准确率与白盒对抗鲁棒性（%）。最佳结果以粗体标出。

在 CIFAR-100 上，LFCM 在 PGD-20 上取得所有方法中最高的 33.14%（较 AT 提升 0.29%，较 HFDR 提升 0.15%），CW-20 与 HFDR 持平（31.18%），AutoAttack 达到 29.82%，优于 AT（29.57%）并接近 HFDR（29.84%）。从权衡指标看，LFCM 的 $\Delta$ Clean$-$AA 为 31.45%，低于 AT（32.41%）和 HFDR（31.46%），在清洁准确率与鲁棒性的权衡上仍为最优。LFCM 在 CIFAR-100 上清洁准确率相对 AT 的下降幅度（-0.71%）大于 CIFAR-10（-0.04%），这源于默认 codebook 大小 $K=64$ 与 CIFAR-100 的 100 个类别之间存在的容量不匹配，我们在 4.3.3 节对此进行专门验证。总体而言，LFCM 在两个数据集上均取得了最优的 PGD-20 鲁棒性与最优的清洁-鲁棒权衡，验证了低频规范化方法的跨数据集有效性。

## 4.3 消融实验

### 4.3.1 Codebook 大小的影响（CIFAR-10）

为探究 codebook 大小对模型性能的影响，我们在固定 $w_{\text{canon}}=0.5$ 的条件下，设置 $K \in \{32, 64, 128\}$ 进行实验，结果如表 3 所示。

| K | Clean | PGD-20 | CW-20 | AA | $\Delta$ Clean$-$AA |
|:-:|:-----:|:------:|:-----:|:--:|:-------------------:|
| 32 | 85.44 | 56.81 | 54.97 | 53.41 | 32.03 |
| 64 | **85.61** | **56.90** | **55.50** | **53.67** | **31.94** |
| 128 | 85.47 | 56.44 | 54.85 | 52.74 | 32.73 |

**表 3.** CIFAR-10 上不同 codebook 大小 $K$ 的结果（%）。

实验结果表明，codebook 大小对性能的影响是非单调的。当 $K=64$ 时各项指标均最优，AutoAttack 达到 53.67%。当 $K=32$ 时，规范化原型的表达能力受限，CW-20 与 AutoAttack 分别下降 0.53% 与 0.26%。当 $K=128$ 时，过大的 codebook 导致部分码字在训练中利用率不足，AutoAttack 降至 52.74%（较 K=64 下降 0.93%）。这表明适当大小的 codebook 能够在低频表示的丰富性与码字利用效率之间取得最优平衡。

### 4.3.2 规范化强度的影响（CIFAR-10）

LFCM 的核心机制在于通过规范化对齐损失 $\mathcal{L}_{\text{canon}}$ 约束干净样本与对抗样本在低频编码空间中的一致性。我们在固定 $K=64$ 的条件下，调整 $w_{\text{canon}} \in \{0, 0.25, 0.5, 1.0\}$ 进行消融，结果如表 4 所示。

| $w_{\text{canon}}$ | Clean | PGD-20 | CW-20 | AA | $\Delta$ Clean$-$AA |
|:------------------:|:-----:|:------:|:-----:|:--:|:-------------------:|
| 0（无规范化） | 86.06 | 56.11 | 55.01 | 52.96 | 33.10 |
| 0.25（弱规范化） | 85.48 | 56.31 | 55.32 | 53.28 | 32.20 |
| 0.5（默认） | 85.61 | **56.90** | **55.50** | **53.67** | **31.94** |
| 1.0（强规范化） | **86.30** | 56.03 | 54.79 | 52.93 | 33.37 |

**表 4.** CIFAR-10 上不同规范化强度 $w_{\text{canon}}$ 的结果（%）。

当 $w_{\text{canon}}=0$（完全移除规范化损失，codebook 仅用于低频特征重构）时，模型清洁准确率最高（86.06%），但对抗鲁棒性最弱，AutoAttack 仅为 52.96%，清洁-鲁棒性差距高达 33.10%。当 $w_{\text{canon}}=0.5$ 时，模型在鲁棒性指标上全面最优，且清洁-鲁棒性差距缩至最小（31.94%）。当进一步增大至 $w_{\text{canon}}=1.0$ 时，模型清洁准确率有所回升（86.30%），但鲁棒性退化至 52.93%，表明过强的规范化约束会过度压缩低频编码空间，损害模型对扰动中低频成分的容忍能力。上述结果表明低频规范化存在最优强度区间：过弱无法充分对齐干净与对抗样本的低频表示，过强则过度约束低频编码空间，适中的强度（$w_{\text{canon}}=0.5$）在保持表征灵活性的同时有效实现规范化目的。

### 4.3.3 Codebook 容量与数据集复杂度的匹配（CIFAR-100）

4.2.2 节观察到默认 $K=64$ 在 CIFAR-100 上限制了 LFCM 的清洁准确率，我们据此推断：codebook 容量应与数据集类别数相匹配。为验证这一假设，我们在 CIFAR-100 上进一步训练了 $K \in \{128, 256, 512\}$ 的 LFCM 模型，并以其在 CIFAR-100-C 上的 OOD 平均准确率进行对比（白盒对抗评估待补），结果如表 5 所示。

| K | Clean Acc（最终） | OOD Mean Acc | OOD Δ vs K=64 |
|:-:|:-----------------:|:------------:|:-------------:|
| 64 | 61.36 | 50.87 | — |
| 128 | 60.89 | **51.31** | +0.44 |
| 256 | 61.19 | 51.27 | +0.40 |
| 512 | 60.92 | 50.71 | -0.16 |

**表 5.** CIFAR-100 上不同 codebook 大小的 OOD 平均准确率（%）。

增大 codebook 容量显著改善了模型的 OOD 鲁棒性：$K=128$ 与 $K=256$ 的 OOD 平均准确率分别达到 51.31% 和 51.27%，均超过了 AT（51.21%）与 HFDR（50.82%），也明显优于 $K=64$（50.87%）。而 $K=512$ 时性能回落（50.71%），再次印证了码字利用率与容量之间的平衡关系。值得注意的是，分析各类损坏可以发现，增大 codebook 容量的增益呈现高度一致的低频偏向性：以 $K=64$ 到 $K=256$ 为例，在 13 种低频损坏上 LFCM 全部获得提升（如 frost 从 47.17% 提升至 50.35%，snow 从 53.08% 提升至 54.90%，brightness 从 55.98% 提升至 57.69%），而在 6 种高频损坏上无系统性变化（如 gaussian noise 从 55.38% 降至 55.10%，shot noise 从 56.79% 降至 56.22%）。这一结果从容量维度进一步验证了 LFCM 的低频机制：更大的 codebook 提供更丰富的低频规范化原型，从而增强模型在低频损坏场景下的鲁棒性，而对高频特征几乎无影响。

## 4.4 OOD 鲁棒性评估

为验证 LFCM 低频规范化在分布外（Out-of-Distribution, OOD）场景下的泛化能力，我们在 CIFAR-10-C 与 CIFAR-100-C 上进行了系统的评估。为探究模型对不同频率特性损坏的差异化表现，我们将 19 种损坏按频率特征划分为两组：

- **低频组（Low-Frequency Group）**：以全局平滑扰动为主，包括模糊类（defocus blur、gaussian blur、motion blur、zoom blur）、天气与光照类（brightness、fog、frost、snow、saturate、contrast）以及数字化退化类（pixelate、elastic transform、JPEG compression），共 13 种损坏。
- **高频组（High-Frequency Group）**：以局部噪声和纹理破坏为主，包括 gaussian noise、shot noise、impulse noise、speckle noise、glass blur 与 spatter，共 6 种损坏。其中 glass blur 涉及局部玻璃纹理的折射扭曲，产生显著的高频局部纹理变化，故归类于高频组。

### 4.4.1 CIFAR-10-C 上的结果

表 6 展示了各方法在 CIFAR-10-C 上的完整评估结果。

| 损坏类型 | 分组 | ST | AT | HFDR | LFCM |
|:---------|:---:|:--:|:--:|:----:|:----:|
| brightness | L | 65.81 | 82.57 | 82.69 | **83.80** |
| contrast | L | 50.70 | 42.57 | **43.00** | 42.69 |
| defocus_blur | L | 60.57 | 80.21 | **80.93** | 80.55 |
| elastic_transform | L | 58.82 | 79.17 | **79.66** | 79.46 |
| fog | L | 63.10 | 58.79 | **59.83** | 59.16 |
| frost | L | 59.14 | 75.30 | 75.56 | **77.99** |
| gaussian_blur | L | 57.94 | 77.94 | **78.69** | 78.50 |
| jpeg_compression | L | 63.78 | 83.50 | **83.79** | 83.18 |
| motion_blur | L | 53.04 | 76.10 | **76.84** | 76.73 |
| pixelate | L | 58.37 | 83.29 | **83.62** | 83.36 |
| saturate | L | 61.43 | 82.88 | **83.13** | 82.76 |
| snow | L | 56.32 | 80.01 | 80.26 | **80.92** |
| zoom_blur | L | 57.19 | 78.82 | **79.76** | 79.35 |
| **低频组平均** | | 58.94 | 75.47 | 75.98 | **76.03** |
| gaussian_noise | H | 50.29 | 82.20 | **82.79** | 81.57 |
| shot_noise | H | 55.17 | 83.06 | **83.69** | 82.55 |
| impulse_noise | H | 45.36 | 76.54 | **77.57** | 76.27 |
| speckle_noise | H | 55.51 | 82.94 | **83.70** | 82.36 |
| glass_blur | H | 48.26 | 78.74 | **79.37** | 78.14 |
| spatter | H | 59.15 | 81.79 | **82.32** | 81.57 |
| **高频组平均** | | 52.29 | 80.88 | **81.57** | 80.41 |
| **总体平均** | | 56.84 | 77.18 | **77.75** | 77.42 |

**表 6.** CIFAR-10-C 各损坏类型上的准确率（%）。L 表示低频组，H 表示高频组。最佳结果以粗体标出。

**低频组分析。** 在低频组的 13 种损坏上，LFCM 以 76.03% 的平均准确率取得最优，优于 HFDR（75.98%）与 AT（75.47%）。相较于 AT 基线，LFCM 的提升为 0.56 个百分点，高于 HFDR 的 0.51 个百分点，LFCM 在低频损坏场景下的增益更为突出。LFCM 的优势在若干代表性低频损坏上尤为显著：在 frost 损坏上，LFCM（77.99%）较 AT（75.30%）提升 2.69%，较 HFDR（75.56%）提升 2.43%；在 brightness 损坏上，LFCM（83.80%）较 AT 提升 1.23%；在 snow 损坏上，LFCM（80.92%）较 AT 提升 0.91%。上述损坏均以全局平滑的频率变化为主要特征，LFCM 在这些场景下的突出表现验证了低频规范化的有效性：通过 codebook 为低频特征提供结构化的规范化表示，模型能够更稳定地处理分布外场景下的低频信号变化。

**高频组分析。** 在高频组的 6 种损坏上，HFDR 以 81.57% 取得最优，AT 为 80.88%，LFCM 为 80.41%。LFCM 在 impulse noise（76.27% vs. 77.57%）、speckle noise（82.36% vs. 83.70%）等高频噪声损坏上弱于 HFDR，但仍与 AT 保持相近水平。这一现象与 LFCM 的设计机制吻合：LFCM 对低频分量进行规范化，而对高频局部噪声，基于全局池化编码的 codebook 难以捕获精细的局部高频纹理变化，因此高频损坏下的表现略逊于专注于高频处理的 HFDR。

**对比分析。** LFCM 在低频组和高频组上的差异化表现揭示了其内在的频率偏向性：通过引入 codebook 对低频特征进行规范化，LFCM 获得了低频 OOD 场景下的鲁棒性增益，同时在高频场景下与 AT 基线持平。AT 与 HFDR 天然倾向于利用高频特征，而 LFCM 的低频规范化弥补了对抗训练对低频特征的依赖中缺乏结构约束的不足，在低频损坏上实现了更好的泛化。

### 4.4.2 CIFAR-100-C 上的结果

表 7 展示了各方法在 CIFAR-100-C 上的评估结果。

| 损坏类型 | 分组 | AT | HFDR | LFCM (K=64) |
|:---------|:---:|:--:|:----:|:----:|
| brightness | L | **57.41** | 56.14 | 55.98 |
| contrast | L | **25.46** | 23.31 | 24.29 |
| defocus_blur | L | **55.58** | 54.60 | 55.12 |
| elastic_transform | L | **54.10** | 53.58 | 53.80 |
| fog | L | **35.15** | 33.47 | 34.30 |
| frost | L | **50.07** | 48.40 | 47.17 |
| gaussian_blur | L | **53.42** | 52.14 | 52.82 |
| jpeg_compression | L | **58.58** | 58.13 | 58.01 |
| motion_blur | L | **52.13** | 50.70 | 51.00 |
| pixelate | L | **58.91** | 58.33 | 58.75 |
| saturate | L | 50.92 | **51.13** | 50.71 |
| snow | L | **54.25** | 53.73 | 53.08 |
| zoom_blur | L | **54.34** | 53.29 | 53.76 |
| **低频组平均** | | **50.79** | 49.77 | 49.91 |
| gaussian_noise | H | 54.68 | 55.32 | **55.38** |
| shot_noise | H | 55.89 | 56.68 | **56.79** |
| impulse_noise | H | 39.61 | **42.22** | 41.47 |
| speckle_noise | H | 54.98 | 55.97 | **56.05** |
| glass_blur | H | 52.73 | **53.32** | 52.99 |
| spatter | H | 54.70 | **55.19** | 55.06 |
| **高频组平均** | | 52.10 | **53.12** | 52.96 |
| **总体平均** | | **51.21** | 50.82 | 50.87 |

**表 7.** CIFAR-100-C 各损坏类型上的准确率（%）。L 表示低频组，H 表示高频组。最佳结果以粗体标出。

在 CIFAR-100-C 上，默认配置的 LFCM（K=64）总体平均为 50.87%，略高于 HFDR（50.82%）但低于 AT（51.21%），且低频组平均（49.91%）未能超越 AT（50.79%）。这一结果与 CIFAR-10-C 上的结论表面上不一致，但实际上可以从 codebook 容量与数据集复杂度的匹配关系得到解释，且与 4.3.3 节的消融结论相互印证：CIFAR-100 包含 100 个类别，远超默认 codebook 容量（$K=64$），低频规范化原型不足以刻画类别间丰富的低频语义差异，导致低频规范化效力受限。当我们依据 4.3.3 节将 codebook 增大至 $K=128$ 或 $K=256$ 后，LFCM 的 OOD 平均准确率提升至 51.31% 与 51.27%，反超 AT（51.21%）与 HFDR（50.82%），且增益高度集中于低频损坏。与此同时，即便在 $K=64$ 的容量受限条件下，LFCM 在高频组（52.96%）仍优于 AT（52.10%），并在 gaussian noise、shot noise、speckle noise 等噪声损坏上取得全部方法中的最优结果。

综合 CIFAR-10-C 与 CIFAR-100-C 的实验结果可以得出：LFCM 的低频规范化在 codebook 容量与数据集复杂度相匹配时，能够在低频 OOD 场景下带来显著且一致的鲁棒性增益，且该增益不依赖对高频特征的牺牲；而当容量不足时，其 OOD 优势被削弱，但白盒对抗鲁棒性（4.2.2 节）依然保持最优。这一致性进一步验证了 LFCM 低频规范化机制的有效性与可靠性。

## 4.5 不同骨干网络上的初步验证

为初步验证 LFCM 对骨干网络的泛化性，我们在更轻量的 ResNet-18 骨干（CIFAR-10 任务）上训练了 LFCM 与各对比方法。由于各方法的正式对抗测试（PGD-20、CW-20、AA）尚未完成，此处报告训练过程中验证集上的最优鲁棒准确率（PGD-10 攻击，$\epsilon=8/255$），结果如表 8 所示。

| 方法 | Best Robust Acc（验证集） | At Epoch | Final Clean Acc |
|:-----|:--------------------------:|:--------:|:----------------:|
| AT | 55.12 | 105 | 82.15 |
| HFDR | **55.95** | 106 | 82.07 |
| LFCM (K=64) | 55.40 | 106 | 81.42 |
| Natural | 21.53 | 102 | 93.09 |

**表 8.** ResNet-18 骨干上训练验证集指标（%）。

在 ResNet-18 骨干上，LFCM 的最优鲁棒准确率（55.40%）高于 AT（55.12%），与 HFDR（55.95%）接近，表明 LFCM 在参数规模更小的骨干网络上同样能够有效提升鲁棒性，且并未引入对骨干网络结构的强依赖。考虑到 HFDR 本身是 LFCM 的频率解耦基础模块，LFCM 在两种骨干上均保持"不弱于 AT、接近 HFDR"的鲁棒性水平，说明低频规范化的增益具有跨骨干的一致性。
