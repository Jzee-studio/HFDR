# 实验部分

## 4.1 实验设置

**数据集与模型。** 所有实验均在 CIFAR-10 数据集上进行。CIFAR-10 包含 10 个类别，训练集 50,000 张图像，测试集 10,000 张图像，图像分辨率为 32×32。OOD 鲁棒性评估在 CIFAR-10-C 数据集上进行，该数据集包含 19 种不同类型的图像损坏，每种损坏分为 5 个严重等级。本文采用骨干网络 WideResNet-34-10（WRN-34-10），包含 34 层卷积，宽度因子为 10。

**训练设置。** 所有对抗训练方法均采用 PGD-10 生成对抗样本进行训练，扰动强度 $\epsilon = 8/255$，步长 $\alpha = 2/255$，迭代步数为 10。优化器使用 SGD，动量为 0.9，权重衰减为 $5\times10^{-4}$，初始学习率为 0.1，batch size 为 128。训练总轮数为 110 个 epoch，学习率在第 100 和 105 个 epoch 分别衰减为原来的 0.1 倍。Natural 训练（ST）不使用对抗样本，其余设置保持一致。

**LFCM 训练策略。** LFCM 采用三阶段训练范式：第一阶段（第 1-30 epoch）为预热阶段，仅使用交叉熵损失与掩码约束损失进行训练，不引入规范化损失；第二阶段（第 31-60 epoch）逐步引入 commitment 损失与多样性损失，学习 codebook 的低频编码表示；第三阶段（第 61-110 epoch）引入规范化对齐损失，以 $w_{\text{canon}}=0.5$（baseline 设置）的权重迫使干净样本与对抗样本的低频编码趋于一致。温度参数 $\tau$ 从初始值 2.0 线性退火至 0.5（第 30-100 epoch），编码分配逐步由软量化过渡至硬量化。每 5 个 epoch 检测并重置死码，死码判定阈值为 EMA 聚类大小小于 2。LFCM 的损失函数为：

$$
\mathcal{L}_{\text{LFCM}} = \mathcal{L}_{\text{CE}} + \lambda_{\text{mask}} \mathcal{L}_{\text{mask}} + \lambda_{\text{commit}} \mathcal{L}_{\text{commit}} + \lambda_{\text{div}} \mathcal{L}_{\text{div}} + w_{\text{canon}} \mathcal{L}_{\text{canon}}
$$

其中 $\mathcal{L}_{\text{CE}}$ 为交叉熵损失，$\mathcal{L}_{\text{mask}}$ 为 HFDR 的频率分离掩码约束损失，$\mathcal{L}_{\text{commit}}$ 和 $\mathcal{L}_{\text{div}}$ 分别为 codebook 的承诺损失与多样性损失，$\mathcal{L}_{\text{canon}}$ 为低频规范化对齐损失。

**评估协议。** 白盒对抗鲁棒性评估采用以下攻击方法：PGD-1（单步攻击）、PGD-20（20 步迭代）、PGD-100（100 步迭代）、CW-20（20 步 C&W 攻击）以及 AutoAttack（AA），所有攻击的扰动强度 $\epsilon = 8/255$。OOD 鲁棒性评估在 CIFAR-10-C 的 19 种损坏类型上测试清洁准确率（不施加对抗扰动），以评估模型在分布外损坏下的泛化能力。所有实验均选取验证集上鲁棒准确率最优的模型进行最终测试。

## 4.2 标准对抗鲁棒性对比

表 1 展示了各方法在 CIFAR-10 测试集上的清洁准确率与白盒对抗鲁棒性。Natural 训练方法在清洁图像上仅取得 66.46% 的准确率（本文未采用标准数据增强，故 ST 基线相对较低），在 AutoAttack 下骤降至 16.35%，表明其对对抗攻击的高度脆弱性。AT 方法通过 PGD-10 对抗训练显著提升了鲁棒性，AutoAttack 下达到 53.61%，同时保持了 85.65% 的清洁准确率。HFDR 在 AT 基础上引入高频特征解耦与重校准模块，将清洁准确率提升至 85.90%，AutoAttack 准确率为 53.56%，展示了频率感知特征增强对鲁棒性的正面作用。

| 方法 | Clean | PGD-1 | PGD-20 | PGD-100 | CW-20 | AA |
|:-----|:-----:|:-----:|:------:|:-------:|:-----:|:--:|
| Natural | 66.46 | 35.52 | 20.45 | 20.12 | 19.41 | 16.35 |
| AT | 85.65 | 80.89 | 56.51 | 56.03 | 55.03 | 53.61 |
| HFDR | 85.90 | 81.20 | 56.34 | 55.71 | 54.83 | 53.56 |
| LFCM (K=64) | 85.61 | 81.16 | **56.90** | **56.02** | **55.50** | **53.67** |

**表 1.** CIFAR-10 上各方法的清洁准确率与白盒对抗鲁棒性（%）。最佳结果以粗体标出。AA 表示 AutoAttack。

本文提出的 LFCM（baseline，WRN-34-10，K=64，$w_{\text{canon}}=0.5$）在 PGD-20、PGD-100、CW-20 和 AutoAttack 四项指标上均取得最优结果，分别达到 56.90%、56.02%、55.50% 和 53.67%。相较于 AT 基线，LFCM 在 AutoAttack 下提升了 0.06 个百分点；相较于 HFDR，LFCM 在保持相近清洁准确率（85.61%）的同时，将 AutoAttack 准确率从 53.56% 提升至 53.67%（+0.11%），同时在 PGD-20 和 CW-20 上分别取得 0.56% 和 0.67% 的提升。值得关注的是，LFCM 的清洁准确率与 AutoAttack 准确率之差（$\Delta$ Clean$-$AA）为 31.94%，低于 AT（32.04%）和 HFDR（32.34%），表明 LFCM 通过低频规范化在清洁准确率与鲁棒性之间取得了更优的权衡。这些结果表明，对低频分量进行规范化处理能够在不牺牲清洁准确率的前提下，进一步提升模型的对抗鲁棒性。

## 4.3 消融实验

### 4.3.1 Codebook 大小的影响

为探究 codebook 大小对模型性能的影响，我们在固定 $w_{\text{canon}}=0.5$ 的条件下，分别设置 codebook 大小 $K \in \{32, 64, 128\}$ 进行实验。实验结果如表 2 所示。

| K | Clean | PGD-20 | CW-20 | AA | $\Delta$ Clean$-$AA |
|:-:|:-----:|:------:|:-----:|:--:|:-------------------:|
| 32 | 85.44 | 56.81 | 54.97 | 53.41 | 32.03 |
| 64 | **85.61** | **56.90** | **55.50** | **53.67** | **31.94** |
| 128 | 85.47 | 56.44 | 54.85 | 52.74 | 32.73 |

**表 2.** 不同 codebook 大小 $K$ 的实验结果（%）。

实验结果表明，codebook 大小对模型性能存在非单调影响。当 $K=64$ 时，模型在各项指标上均取得最佳性能，AutoAttack 准确率达到 53.67%，清洁-鲁棒性差距为 31.94%。当 $K=32$ 时，由于规范化原型的表达能力受限，模型在 CW-20 和 AutoAttack 上分别下降 0.53% 和 0.26%。当 $K=128$ 时，过大的 codebook 导致部分码字在训练中利用率不足，AutoAttack 准确率降至 52.74%（较 K=64 下降 0.93%），清洁-鲁棒性差距扩大至 32.73%。这一现象表明，适当大小的 codebook（K=64）能够在低频表示的丰富性与码字利用效率之间取得最优平衡。

### 4.3.2 规范化强度的影响

LFCM 的核心机制在于通过规范化对齐损失 $\mathcal{L}_{\text{canon}}$ 约束干净样本与对抗样本在低频编码空间中的一致性。为验证低频规范化强度的作用，我们在固定 $K=64$ 的条件下，调整规范化权重 $w_{\text{canon}} \in \{0, 0.25, 0.5, 1.0\}$ 进行消融实验，结果如表 3 所示。

| $w_{\text{canon}}$ | Clean | PGD-20 | CW-20 | AA | $\Delta$ Clean$-$AA |
|:------------------:|:-----:|:------:|:-----:|:--:|:-------------------:|
| 0（无规范化） | **86.06** | 56.11 | 55.01 | 52.96 | 33.10 |
| 0.25（弱规范化） | 85.48 | 56.31 | 55.32 | 53.28 | 32.20 |
| 0.5（基准） | 85.61 | **56.90** | **55.50** | **53.67** | **31.94** |
| 1.0（强规范化） | 86.30 | 56.03 | 54.79 | 52.93 | 33.37 |

**表 3.** 不同规范化强度 $w_{\text{canon}}$ 的实验结果（%）。

实验结果表明，规范化强度对模型性能存在最优区间。当 $w_{\text{canon}}=0$（即完全移除规范化损失，codebook 仅用于低频特征重构）时，模型清洁准确率最高（86.06%），但对抗鲁棒性最弱，AutoAttack 准确率仅为 52.96%，清洁-鲁棒性差距高达 33.10%。当 $w_{\text{canon}}=0.25$（弱规范化）时，鲁棒性有所提升（AA: 53.28%），但清洁准确率出现下降（85.48%）。当 $w_{\text{canon}}=0.5$（基准设置）时，模型在鲁棒性指标上全面最优，AutoAttack 达到 53.67%，且清洁-鲁棒性差距缩至最小（31.94%）。当进一步增大至 $w_{\text{canon}}=1.0$（强规范化）时，模型清洁准确率有所回升（86.30%），但鲁棒性出现退化，AutoAttack 降至 52.93%，表明过度强调低频规范化可能导致低频编码空间的过约束，限制了模型对对抗扰动中低频特征的容忍能力。

上述消融结果表明，低频规范化存在一个最优强度区间：过弱则无法充分对齐干净与对抗样本的低频表示，过强则会过度约束低频编码空间而损害鲁棒性。适中的规范化强度（$w_{\text{canon}}=0.5$）能够在保持表征灵活性的同时，有效实现低频规范化的目的。

## 4.4 OOD 鲁棒性评估

为验证 LFCM 的低频规范化在分布外（Out-of-Distribution, OOD）场景下的泛化能力，我们在 CIFAR-10-C 数据集上进行了系统的 OOD 鲁棒性评估。CIFAR-10-C 包含 19 种图像损坏类型，为探究 LFCM 对不同频率特性损坏的差异化表现，我们将 19 种损坏按照其频率特征划分为两个组别：

- **低频组（Low-Frequency Group）**：以全局平滑扰动为主，主要包括模糊类（defocus blur、gaussian blur、motion blur、zoom blur、glass blur）、天气与光照类（brightness、fog、frost、snow、saturate、contrast）以及数字化退化类（pixelate、elastic transform、JPEG compression），共 13 种损坏。
- **高频组（High-Frequency Group）**：以局部噪声和纹理破坏为主，主要包括各类噪声（gaussian noise、shot noise、impulse noise、speckle noise）以及溅射（spatter），共 6 种损坏。其中 glass blur 虽名义上为模糊，但涉及局部玻璃纹理的折射扭曲，产生显著的高频局部纹理变化，故归类于高频组。

各方法在 CIFAR-10-C 上的完整评估结果如表 4 所示。

| 损坏类型 | 分组 | ST | AT | HFDR | LFCM |
|:---------|:---:|:--:|:--:|:----:|:----:|
| brightness | L | 65.81 | 82.57 | 82.69 | **83.80** |
| contrast | L | 50.70 | 42.57 | 43.00 | 42.69 |
| defocus_blur | L | 60.57 | 80.21 | **80.93** | 80.55 |
| elastic_transform | L | 58.82 | 79.17 | **79.66** | 79.46 |
| fog | L | 63.10 | 58.79 | **59.83** | 59.16 |
| frost | L | 59.14 | 75.30 | 75.56 | **77.99** |
| gaussian_blur | L | 57.94 | 77.94 | **78.69** | 78.50 |
| jpeg_compression | L | 63.78 | 83.50 | **83.79** | 83.18 |
| motion_blur | L | 53.04 | 76.10 | **76.84** | 76.73 |
| pixelate | L | 58.37 | 83.29 | **83.62** | 83.36 |
| saturate | L | 61.43 | 82.88 | **83.13** | 82.76 |
| snow | L | 56.32 | 80.01 | 80.26 | **80.92** |
| zoom_blur | L | 57.19 | 78.82 | **79.76** | 79.35 |
| **低频组平均** | | 58.94 | 75.47 | 75.98 | **76.03** |
| gaussian_noise | H | 50.29 | 82.20 | **82.79** | 81.57 |
| shot_noise | H | 55.17 | 83.06 | **83.69** | 82.55 |
| impulse_noise | H | 45.36 | 76.54 | **77.57** | 76.27 |
| speckle_noise | H | 55.51 | 82.94 | **83.70** | 82.36 |
| glass_blur | H | 48.26 | 78.74 | **79.37** | 78.14 |
| spatter | H | 59.15 | 81.79 | **82.32** | 81.57 |
| **高频组平均** | | 52.29 | 80.88 | **81.57** | 80.41 |
| **总体平均** | | 56.84 | 77.18 | **77.75** | 77.42 |

**表 4.** CIFAR-10-C 各损坏类型上的清洁准确率（%）。L 表示低频组，H 表示高频组。最佳结果以粗体标出。

从总体平均值来看，HFDR 以 77.75% 取得最优，LFCM 以 77.42% 次之，AT 为 77.18%。然而，从分组视角观察，呈现出明显的差异化特征：

**低频组分析。** 在低频组（13 种损坏）上，LFCM 以 76.03% 的平均准确率取得最优，优于 HFDR（75.98%）和 AT（75.47%）。相较于 AT 基线，LFCM 在低频组的提升为 0.56 个百分点，而 HFDR 的提升为 0.51 个百分点，LFCM 在低频损坏场景下的相对优势更为突出。在若干具有代表性的低频损坏类型上，LFCM 的优势尤为显著：在 frost（霜冻）损坏上，LFCM（77.99%）较 AT（75.30%）提升 2.69%，较 HFDR（75.56%）提升 2.43%；在 brightness（亮度）损坏上，LFCM（83.80%）较 AT（82.57%）提升 1.23%；在 snow（雪花）损坏上，LFCM（80.92%）较 AT（80.01%）提升 0.91%。上述损坏类型均以全局平滑的频率变化为主要特征，LFCM 在这些场景下的突出表现验证了低频规范化机制的有效性——通过 codebook 为低频特征提供结构化的规范化表示，模型能够更稳定地处理分布外场景下的低频信号变化。

**高频组分析。** 在高频组（6 种损坏）上，HFDR 以 81.57% 取得最优，LFCM 为 80.41%，AT 为 80.88%。LFCM 在高频组上相较于 HFDR 存在 1.16 个百分点的差距，尤其在 impulse noise（76.27% vs. 77.57%，-1.30%）、speckle noise（82.36% vs. 83.70%，-1.34%）等高频噪声损坏上表现较弱。这一现象与 LFCM 的设计机制相吻合：LFCM 的核心在于对低频分量进行规范化，对于高频局部噪声，codebook 的全局池化编码方式难以捕获精细的局部高频纹理变化，导致在高频损坏下的泛化能力略逊于专注于高频处理的 HFDR。

**对比分析。** LFCM 在低频组和高频组上的差异化表现揭示了其内在的频率偏向性：LFCM 通过引入 codebook 对低频特征进行规范化，获得了低频 OOD 场景下的鲁棒性增益，但这种增益以高频场景下的轻微性能代价为前提。从频率偏好的视角来看，AT 和 HFDR 的方法天然倾向于利用高频特征，而 LFCM 的低频规范化则弥补了这一不足，在低频损坏上实现了更好的泛化。这一结果从 OOD 视角验证了低频规范化的有效性。

值得指出的是，虽然 LFCM 在总体平均上未超越 HFDR（77.42% vs. 77.75%），但这恰反映出不同方法的频率处理偏好的差异：HFDR 侧重于高频特征的增强与重校准，LFCM 侧重于低频特征的规范化表示。两者在频率维度上形成了互补，而 LFCM 在低频 OOD 场景下的优势为低频频偏问题的缓解提供了新的视角和有效方案。
