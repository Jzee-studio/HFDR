name: HFDR
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.11
  - pip
  - pytorch=2.4.0
  - torchvision=0.19.0
  - pytorch-cuda=12.1
  - numpy
  - pip:
      - easydict
      - pillow
      - pyyaml
      - scipy
      - scikit-learn
      - torchaudio==2.4.0
      - requests
      - tqdm
      - mkl==2024.0